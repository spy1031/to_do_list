<%= render :partial => "form" %>

<%= form_for @list, :url =>lists_path do |f| %>
  <%= f.label :title, "Title" %>
  <%= f.text_field :title %>

  <%= f.label :due_date, "Date" %>
  

  
  <%= f.label :note, "Note" %>
  <%= f.text_area :note %>
  <%= f.submit "Create" %>

<% end %>